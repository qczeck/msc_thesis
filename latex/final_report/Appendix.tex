% !TEX root = final_report.tex
\documentclass[final_report.tex]{subfiles}

% Appendices are not counted towards the 60-page content limit, and are not
% guaranteed to be read. Nothing load-bearing belongs here --- and no source
% code listings, which live in the repository.

\begin{document}
\apxsection{Hyperparameters and run configurations}\label{apx:hyperparameters}
\stub{The full configuration of every run referenced in the report, so that a
reader can reproduce it: patch size, schedule, batch size, loss weights, seeds.}

\apxsection{The pre-registration}\label{apx:pre-registration}
\stub{The analysis plan for the held-out scoring, reproduced verbatim as it was
fixed before the runs started.}

\apxsection{Supplementary tables}\label{apx:supplementary}
\stub{Per-arm tables too detailed for the body, including the intrinsic gates at
each checkpoint and the per-split downstream results.}

\end{document}
