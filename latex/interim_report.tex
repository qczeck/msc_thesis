% =====================================================================
%  RoPART — MSc Computing Interim Report (SCAFFOLD)
%  Maciej Kuczek, Imperial College London, Department of Computing (70081)
%
%  This file is a STRUCTURE-ONLY scaffold. Every section contains a
%  \guide{...} note describing what to write and which references to draw
%  on — it renders as a grey italic "[Writing guide]" block so it is easy
%  to spot and delete. Replace each guide with your own prose, then remove
%  the macro. The body is intentionally NOT written.
%
%  Build:  pdflatex interim_report  ->  bibtex interim_report
%          ->  pdflatex x2
%  Referencing: Vancouver (numbered, in order of appearance). We use
%  unsrtnat as a portable approximation; swap in a vancouver.bst if your
%  TeX distribution ships one (see the \bibliographystyle line below).
% =====================================================================
\documentclass[11pt,a4paper]{report}

\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage[british]{babel}
\usepackage[margin=2.5cm]{geometry}
\usepackage{amsmath,amssymb}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage[numbers,sort&compress]{natbib}
\usepackage[colorlinks=true,linkcolor=black,citecolor=black,urlcolor=blue]{hyperref}
\usepackage{xurl}

% --- Writing-guide macro: delete the macro use as you fill each section ---
\newcommand{\guide}[1]{%
  \par\smallskip
  \noindent{\color{gray!85!black}\small\itshape\textbf{[Writing guide]}~#1}%
  \par\smallskip
}

\begin{document}

% =====================================================================
%  TITLE PAGE
% =====================================================================
\begin{titlepage}
  \centering
  \vspace*{1.5cm}

  {\large Imperial College London \\[0.3em] Department of Computing\par}
  \vspace{2.0cm}

  {\LARGE\bfseries Relative Orientation in Off-Grid \\[0.4em]
   Self-Supervised Learning:\\[0.6em]
   Extending PART from Translation to Rigid Motion\par}
  \vspace{1.0cm}

  {\large\itshape RoPART --- Rotation + PART\par}
  \vspace{2.0cm}

  {\large Maciej Kuczek\par}
  \vspace{0.4cm}
  {\normalsize MSc Computing --- Individual Project (70081)\par}
  \vspace{2.0cm}

  \begin{tabular}{rl}
    Supervisor:    & \textit{[supervisor name]} \\[0.3em]
    Second marker: & \textit{[second-marker name]} \\
  \end{tabular}

  \vfill
  {\normalsize Interim Report\par}
  {\normalsize \today\par}
\end{titlepage}

% =====================================================================
%  TABLE OF CONTENTS
% =====================================================================
\tableofcontents
\clearpage

% =====================================================================
%  CHAPTER 1 — INTRODUCTION
% =====================================================================
\chapter{Introduction}
The deep learning revolution of the last decade was made possible by a combination of three factors. First, advances in graphics processing units (GPUs) and parallel computing allowed for faster processing of large amounts of data. Second, architectural advances such as convolutional neural networks (CNNs), new regularisation techniques, and better activation functions improved the trainability and reliability of models. Third, large amounts of labelled training data allowed the models to actually learn rather than memorise. In 2026, compute is still growing thanks to companies such as Nvidia and AMD. Frontier labs such as OpenAI or DeepMind are hard at work researching new architectures and techniques. Labelled data, however, remains elusive and expensive, often requiring alternative approaches. One such approach is self-supervised learning (SSL), which relies on leveraging unlabelled data and pretext tasks which, when solved, provide a supervisory signal to the network.

There are many different types of pretext tasks - some remove parts of an image, some rotate the image, some remove colour - but the goal is the same: to force the network to learn complex representations of images without a human label. In particular, spatial/position pretext tasks are important as the image composition and the relationship of objects in an image can be a powerful source of information. PART (PAirwise Relative Translations) is one such pretraining method that leverages relative transformations between two image patches. That method predicts relative translation only ($\Delta x,\Delta y$) with rotation left as future work - which is what this project sets out to add.

\section{Problem and context}
Most spatially-aware tasks involve a grid structure. Masked AutoEncoder (MAE) slices an image into a grid of patches and then masks some of them. The pretext task is to then reconstruct the missing patches. DropPos masks some patches on a grid then drops information about absolute patch positions and tasks the network with recovering the missing positions. The pretext task in Doersch et al.'s context prediction tasks the model with predicting the relative position of patches while still being confined to a grid. 

The novel contribution of PART is to sample the patches untethered to a grid and to then predict the translation between two off-grid patches. Geometrically, predicting only $(\Delta x, \Delta y)$ means PART's target space is the translation subgroup of the plane. The natural unfinished step is to predict the full group of planar rigid motions, $\mathrm{SE}(2)$, by adding a relative orientation target $\Delta\varphi$ to each patch pair. The authors themselves flag this as open: they observe that seeing a lip implies a nose above it, but a \emph{rotated} lip implies a \emph{rotated} nose, and leave modelling rotations to future work (their Section~7). This matters because grid-based pretext tasks cannot express continuous geometry of this kind, yet the dominant orientation of a part relative to its neighbours is often the discriminative signal - particularly for orientation-sensitive tasks such as pose estimation or oriented object detection. To the best of current knowledge, no prior work extends a relative off-grid patch-pair pretext task to relative orientation.

\section{Aims and objectives}
The research question can be summarised as follows: does adding a relative orientation target ($\Delta \varphi$) to each patch pair yield representations that perform better on orientation-sensitive downstream tasks without degrading classification? To answer it, the following objectives will be pursued:
\begin{itemize}
    \item Design a new pretext task that adds rotation on top of translation, accounting for artefacts such as missing corners or jagged pixels, and introducing an angle-aware loss function.
    \item Implement the rotation target behind a flag so that the translation-only baseline path is unchanged, keeping any downstream effect cleanly attributable to the orientation channel.
    \item Train a Vision Transformer on the pretext task and characterise the angle channel, under appropriate controls, to verify whether the model is learning and whether its predictions obey the geometry that the true relative orientation must obey.
    \item Evaluate downstream against the translation-only baseline on a classification no-degradation check and an orientation-sensitive task.
\end{itemize}

% =====================================================================
%  CHAPTER 2 — BACKGROUND AND LITERATURE REVIEW
% =====================================================================
\chapter{Background and Literature Review}


\section{Self-supervised representation learning}
\guide{Briefly motivate SSL: learn transferable features from unlabelled images via
a pretext task, then transfer/fine-tune. Place pretext tasks in context and lead
into the spatial family. Keep this short --- it is scene-setting, not the focus.}

Self-supervised learning (SSL) is a learning paradigm where the supervisory tasks is generated from data itself, without needing a human-assigned label \cite{ozbulak2023knowselfsupervisedlearningsurvey}. SSL approaches can be grouped into either generative or discriminative. The goal of generative approaches is to learn distributions over a collection of data in pixel space, whereas discriminative approaches' goal is to learn representations of data with high discriminative power (similarly to supervised approaches, just without human labels). 

\section{Spatial and position-based pretext tasks}
\guide{The lineage that produces PART. Start with relative context prediction
(Doersch et al.~\cite{context2015}) and jigsaw puzzles
(Noroozi \& Favaro~\cite{jigsaw}) as the early patch-spatial pretext tasks; then
the position-prediction / masked-modelling line on transformers: MAE~\cite{mae},
MP3~\cite{mp3}, DropPos~\cite{droppos}. The key axis to draw out: these predict
\emph{absolute} positions or reconstruct masked content on a fixed grid, whereas
PART predicts \emph{relative} geometry between off-grid pairs. This contrast is the
backbone of your argument.}

\section{PART: relative composition of off-grid patches}
\guide{The most important section --- give PART its own detailed treatment, as your
work extends it directly~\cite{part}. Cover: (i)~off-grid continuous patch sampling
(\texttt{v3}) and the absence of position embeddings; (ii)~the pairwise relative
\emph{translation} target $(\Delta x,\Delta y)$; (iii)~the cross-attention relative
encoder trained with MSE; (iv)~the relative-\emph{scale}/aspect extension
$(\Delta w,\Delta h)$ as a proof of concept that the target framework is extensible
(their Table~1), and the reported interference where scale terms delayed
$\Delta x/\Delta y$ convergence; (v)~the negative-symmetry property
$\theta(i\!\to\!j)=-\theta(j\!\to\!i)$ and the patch-uncertainty capability. End by
naming the rotation gap their Section~7 leaves open --- this is your wedge.}

\textbf{PA}irwise \textbf{R}elative \textbf{T}ranslations is a pretraining method that is not based on a grid. Given that this paper is the base of this project, a more detailed treatment is required.

First, given an image, $N$ random patches are extracted, where the top-left coordinates of patch $s$, $(x_s,y_s)$ are drawn uniformly across the image. The patches are of size $P$ (therefore, the bottom-right coordinates are given by $(x_s+P,y_s+P)$). Because the patches are sampled uniformly and off-grid, masking occurs naturally. 

Overall, $N=\frac{H\times W}{P^2}$ patches are sampled and reshaped into the original image size $\hat{I}\in \mathbb{R}^{H\times W\times C}$ where $C$ is the number of channels. 



\section{Rotation in self-supervision and in architectures}
\guide{Two distinct ways rotation has entered vision. (1)~As an SSL \emph{objective}:
RotNet~\cite{rotnet} predicts a single global, discrete image rotation --- an
absolute, image-level invariance task, fundamentally different from continuous,
pairwise, relative orientation between parts. Make this distinction explicit; it is
why RoPART is novel. (2)~As an architectural prior: group-equivariant
CNNs~\cite{gcnn}, $\mathrm{E}(2)$-steerable CNNs~\cite{e2cnn}, and spatial
transformer networks~\cite{stn}. Position your project as \emph{learning} relative
orientation as a target, with steerable architecture noted only as a stretch goal.}

\section{Representing and regressing rotation}
\guide{Justify the design choice for $\Delta\varphi$. Direct angle regression is
discontinuous at $\pm\pi$ and a poor neural target; Zhou et al.~\cite{rotcont} show
why continuous representations are needed, motivating a $(\cos\Delta\varphi,
\sin\Delta\varphi)$ target with an angle-aware loss. Note that this also dictates how
intrinsic angular error and the symmetry/consistency metrics must be computed.}

\section{Multi-objective learning and loss balancing}
\guide{Adding $\Delta\varphi$ alongside $(\Delta x,\Delta y)$ (and possibly scale)
is a multi-task regression. Discuss why balancing matters --- PART already reports
inter-channel interference --- and introduce uncertainty-based task
weighting~\cite{kendall} as the principled option to compare against fixed/scheduled
weights. This frames loss weighting as a planned experimental variable, not an
afterthought.}

\section{Orientation-sensitive downstream tasks}
\guide{Set up the evaluation targets. Explain why orientation matters in aerial,
microscopy and document domains, and introduce the oriented-detection benchmark
DOTA~\cite{dota} with an oriented detector such as Oriented R-CNN~\cite{orcnn} as a
realistic application, alongside a lighter relative-pose probe. Be honest about
scope: these motivate the \emph{one} real downstream task you will do properly.}

\section{Notation and preliminaries}
\guide{Optional but recommended (the spec explicitly allows a notation/theory
section). Define the groups you invoke --- translation subgroup, $\mathrm{SE}(2)$,
and $\mathrm{Sim}(2)$ once scale is included --- and fix notation for patch pairs
$(i,j)$, the relative target channels, and the symmetry/compositional-consistency
relations. Keep it to the minimum the later chapters actually use.}

% =====================================================================
%  CHAPTER 3 — TECHNICAL ACHIEVEMENTS / PRELIMINARY WORK
% =====================================================================
\chapter{Preliminary Work and Feasibility}

\guide{The spec notes MSc Computing students are unlikely to have substantial
experimental results yet, so keep this chapter short and honest --- it is about
\emph{feasibility}, not findings. Report only what is genuinely done. Do not
over-claim.}

\section{Reproducing the PART pipeline on a modern stack}
\guide{Describe getting the upstream PART implementation running on the current
stack (Python~3.12, torch~2.x) and the device strategy (Apple-Silicon MPS for
local development, CUDA/SLURM for full pretraining). Mention the baseline smoke run
on CIFAR-100 with ViT-S as a feasibility check. Keep specifics light at interim
stage; the detailed engineering belongs in the final report.}

\section{Towards a translation-only baseline}
\guide{State the build-order principle: add geometry one channel-group at a time,
starting from a clean translation-only $(\Delta x,\Delta y)$ baseline (scale
disabled) as the control everything is measured against, with translational
negative symmetry as a correctness gate before any rotation work. Note this is
planned/in-progress rather than completed if that is the case.}

% =====================================================================
%  CHAPTER 4 — PROJECT PLAN
% =====================================================================
\chapter{Project Plan}

\guide{The forward-looking chapter. Make the plan concrete and measurable, and tie
it back to the objectives in Chapter~1. A reader should finish this chapter knowing
exactly what you will build, how you will know it worked, and what could go wrong.}

\section{Approach and phases}
\guide{Lay out the phased plan (reuse the proposal's four-phase structure):
Phase~0 baseline reproduction + translation-only control; Phase~1 the
rotation-aware objective $(\Delta x,\Delta y,\Delta\varphi)$ with the interpolation
control; Phase~2 ablations (angle representation, frame convention, loss weighting,
and --- last --- whether scale helps); Phase~3 downstream evaluation; Phase~4
synthesis and writing. Emphasise that every addition is flag-gated so the baseline
path is unchanged, which keeps any downstream effect cleanly attributable.}

\section{Timeline}
\guide{Give a week-by-week or phase-by-phase timeline to the deadlines (poster
2026-09-03, final report 2026-09-10). A simple table is fine; consider
\texttt{pgfgantt} for a chart. Fill in the milestone rows below.}

\begin{table}[h]
  \centering
  \begin{tabular}{@{}lll@{}}
    \toprule
    Phase & Weeks & Milestone / deliverable \\
    \midrule
    Phase 0 & \textit{[Wk]} & \textit{[baseline reproduced; translation-only control]} \\
    Phase 1 & \textit{[Wk]} & \textit{[rotation pretext training at scale]} \\
    Phase 2 & \textit{[Wk]} & \textit{[ablations + intrinsic metrics]} \\
    Phase 3 & \textit{[Wk]} & \textit{[downstream evaluation]} \\
    Phase 4 & \textit{[Wk]} & \textit{[synthesis, poster, report]} \\
    \bottomrule
  \end{tabular}
  \caption{Project timeline (to be completed).}
\end{table}

\section{Evaluation and success criteria}
\guide{Define what success looks like --- this is explicitly asked for. Separate
\emph{intrinsic} metrics (angular error on $\Delta\varphi$; rotational negative
symmetry $\Delta\varphi(i\!\to\!j)=-\Delta\varphi(j\!\to\!i)$; compositional
consistency) from \emph{downstream} outcomes (a classification no-degradation check
vs.\ the translation-only baseline; a synthetic orientation probe; one real
orientation-sensitive task). State the comparison baseline and what counts as a
positive result.}

\section{Risks and mitigations}
\guide{Address the validity threats head-on --- markers reward this. The big one is
the \emph{interpolation confound}: rotating and resampling a small patch leaves a
blur signature correlated with $\varphi$, so any positive $\Delta\varphi$ result
needs a matched-interpolation control before it can be claimed as genuine learning.
Also cover orientation recoverability/well-posedness (the signal is sparser than
translation and content-dependent --- potentially turned into an orientation-
uncertainty contribution) and the compute ceiling (full ImageNet-1K ViT-B
pretraining is out of scope; CIFAR-100 / ViT-S is the workhorse).}

\section{Ethical considerations}
\guide{Brief but required if relevant. This project uses standard public vision
benchmarks (CIFAR-100, optionally ImageNet-100, and an aerial-imagery set for the
downstream task) and no human-subjects or personal data, so Imperial ethical
approval is most likely \emph{not} required --- state this explicitly and note any
caveats (e.g.\ licensing/usage terms of the aerial dataset). Confirm against your
ethics review checklist.}

% =====================================================================
%  REFERENCES — Vancouver (numbered, in order of appearance)
% =====================================================================
\bibliographystyle{unsrtnat} % portable Vancouver-like numbering; swap for vancouver.bst if available
\bibliography{references}

% =====================================================================
%  APPENDICES (optional — likely unnecessary for this short document)
% =====================================================================
% \appendix
% \chapter{Supplementary material}
% \guide{Only if needed: extra figures, full configs, derivations.}

\end{document}
